# Ride-flow domain contracts

These models mirror the existing ride-flow HTTP response shapes. They intentionally keep server lifecycle values as strings during the architecture correction and do not change controller behavior.
